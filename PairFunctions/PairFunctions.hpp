#ifndef __PAIR_FUNCTIONS__
#define __PAIR_FUNCTIONS__

#include <iostream>
#include <vector>

using namespace std;

template <typename T>
void PrintPair(pair<T, T> &pairList);
template <typename T>
void PrintPairVector(vector<pair<T, T>> &vectorList);

#endif // __PAIR_FUNCTIONS__